{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your project description",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "start": "cd server && python app.py",
    "heroku-postbuild": "cd client && npm install --no-cache && npm run build",
    "clean": "rm -rf node_modules client/node_modules"
  },
  "cacheDirectories": [],
  "dependencies": {}
}
